#include<iostream>
using namespace std;

// Spanning Tree (ST)
/*
    -> Convert graph to a tree (no cycle).
    -> Every node of the tree should be reachable by every other node.
    -> Tree would have 'n' nodes and 'n - 1' edges.

    -> MST is a ST where the sum of all weights of edges in the tree should be minimum.
    -> There are different algo's to make a minimum spanning tree:
*/

// 1) Prim's Algorithm
/*
    -> This algo states that we'll build the tree from scratch.

*/


int main(){

    return 0;
}